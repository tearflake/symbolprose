<start> := (GRAPH (NAME <ATOMIC>) (DEFINE <edge>+))
         | (IMPORT <ATOMIC> <ATOMIC>)
         | (IMPORT <ATOMIC>)

<edge> := (EDGE (SOURCE <ATOMIC>) (INSTR <instruction>+)? (TARGET <ATOMIC>))
        | <start>

<instruction> := (TEST <ANY> <ANY>)
               | (ASGN <ATOMIC> <ANY>)

